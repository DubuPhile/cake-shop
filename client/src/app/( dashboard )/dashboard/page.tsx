import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard",
  description: "Metrics",
};

export default function Dashboard() {
  return (
    <div>
      <h2>Dashboard</h2>
    </div>
  );
}
